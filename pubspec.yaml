name: clean_dart
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.2-stable <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  dartz:
  dio: ^5.1.1
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  flutter_modular: ^5.0.3
  get: ^4.6.5

dev_dependencies:

  flutter_lints: ^2.0.0
  modular_test: ^2.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0

flutter:

  uses-material-design: true


  assets:
    - assets/images/github.svg

